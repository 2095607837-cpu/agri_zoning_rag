# APO 优化 V3 改写 Prompt 报告（155 优化 / 25 验证门槛）

生成时间：2026-07-08 | 优化集：`data/golden_rewrite_test.json`(155) | 验证集：`data/golden_rewrite_val.json`(25)
目标函数：GT `term_weighted_recall`（0.45 must+0.30 core+0.10 precision+0.10 important+0.05 optional）| 验证门槛 ε=0.005

## 变更日志

| 日期 | 变更 |
|------|------|
| 2026-07-08 | APO 首次双集优化：产生更优 prompt（已存 `data/candidate_best_prompt_v3.txt`，未自动 apply） |

---

## 一、核心结果

| | 优化集155 加权召回 | 验证集25 加权召回 |
|---|---|---|
| Baseline V3 | 0.7557 | 0.5173 |
| 最优 (R1_candidate_1) | 0.7690 | 0.5313 |
| Δ | +0.0133 | +0.0140 |

> 验证集未回归（Δ25=+0.0140 ≥ -0.005），优化集提升 +0.0133。最优 prompt 待人工审阅后决定是否 apply。

---

## 二、逐轮候选

### Round 1

| 候选 | 方向 | 155 | 25 | 155提升 | 25达标 |
|------|------|-----|-----|--------|--------|
| R1_candidate_1 | 针对 [口语/同义词未映射到专业术语]（12 题） | 0.7690 | 0.5313 | ✅ | ✅ |

### Round 2

| 候选 | 方向 | 155 | 25 | 155提升 | 25达标 |
|------|------|-----|-----|--------|--------|
| R2_candidate_1 | 针对 [口语/同义词未映射到专业术语]（12 题） | 0.7681 | 0.5153 | ❌ | ❌ |

---

## 三、验证集 LLM-Judge 二次确认（val-25，apples-to-apples）

加权召回是主目标；此处再用 LLM-Judge 综合评分 RQ 在同一路径下对比 V3 与候选，确认未回归。

| 指标（val-25） | V3 | 候选 R1_candidate_1 | Δ |
|---|---|---|---|
| GT 加权召回 | 0.5173 | 0.5313 | +0.0140 |
| LLM-Judge RQ（25 题均值） | 6.98 | 7.10 | +0.12 |
| LLM-Judge RQ（有效题均值，去 eval_error） | 7.59 | 8.07 | +0.48 |

> eval_error 数（V3=2 / 候选=3）为 Judge JSON 解析噪声，逐次波动，与改写质量无关。候选在加权召回与 LLM-Judge 两个口径上均不低于 V3，验证集无回归。

---

## 四、方法

- 优化信号仅取自 155 生成集；每个候选都在 25 人工精标集上回归验证，只有『155 加权召回提升 且 25 不低于 baseline−ε』才晋升，保证质量不下滑。
- fast 模式（GT 加权召回，无 LLM-Judge），确定性、低成本。
- 候选由 `generate_candidates_v2` 基于 155 低召回失败样本生成，并校验保留 `{query}` 占位与 JSON `keywords` 输出契约。
- **未修改** `query_rewriter.py`；最优 prompt 原文见 `data/candidate_best_prompt_v3.txt`。
